import { Component } from "@/components/ui/animated-3d-card";

/**
 * Upstream's demo, kept as the component's reference rendering — the same role
 * `odometer-demo.tsx` and `progress-bar-demo.tsx` play. Nothing routes to it;
 * it is here so the card can be seen on its own, in its own palette, without
 * reading it through the services grid that adapts it.
 */
export default function DemoOne() {
  return <Component />;
}
